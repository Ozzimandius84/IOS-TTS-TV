{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The one window, and only what a window needs. The shell reaches its own files over frank:// and Studio over fetch; the ONE command it calls is sync_discover (job 26, 6 Sep) -- the Bonjour browse behind the Sync button -- through window.TTSTVHost, which lib.rs injects. Nothing else beyond core:default is granted.",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default",
    "allow-sync-discover"
  ]
}
